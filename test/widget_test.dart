import 'package:flutter_test/flutter_test.dart';

// Widget tests for LearnQuest
void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — add real tests as features are built out.
    expect(true, isTrue);
  });
}
